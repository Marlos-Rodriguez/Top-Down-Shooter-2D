using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathFandingTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Pathfinding pathfinding = new Pathfinding(10, 10);
    }

}
